<UserControl x:Class="project_navigator.views.common.Logo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="100"
             Width="100">
    <Viewbox Stretch="Uniform"
             SnapsToDevicePixels="True">
        <Grid>
            <Path Fill="{StaticResource LogoColor}"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  Data="M 20,0 L 80,0 Q 100,0 100,20 L 100,80 Q 100,100 80,100 L 20,100 Q 0,100 0,80 L 0,20 Q 0,0 20,0 Z" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="16 32  0 0">
                <TextBlock Text="Н"
                           FontSize="40"
                           Foreground="White"
                           FontWeight="Bold" />
                <!-- Буква П -->
                <TextBlock Text="П"
                           Foreground="White"
                           FontSize="40"
                           FontWeight="Bold" />
            </StackPanel>
        </Grid>
    </Viewbox>
</UserControl>